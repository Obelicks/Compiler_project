Run flex, bison, and compile by writing the following on the command line:
   make

To run, write the following on the command line:

  make run

To test costum files not names test.sk
  ./compiler (tiger file) > (src file) 2> (log file)

to clean (removes .o and executable files and the standard out and log file):
  make clean
